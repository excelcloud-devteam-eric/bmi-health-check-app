name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read
  security-events: write

env:
  AWS_REGION: us-east-1
  PROJECT_NAME: bmi-health-check-app

jobs:
  test:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r app/requirements.txt -r requirements-dev.txt
      - name: Run tests
        run: PYTHONPATH=. pytest -q

  build-scan-push:
    name: Build, scan, and push image
    needs: test
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
      image_uri: ${{ steps.meta.outputs.image_uri }}
    env:
      IMAGE_TAG: ${{ github.sha }}
      IMAGE_URI: ""
      IMAGE_REF: ""
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_IAM_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify AWS identity
        run: |
          set -euo pipefail
          EXPECTED_ROLE="arn:aws:iam::124666675812:role/github-actions-bmi-health-check"
          CALLER_ARN=$(aws sts get-caller-identity --query Arn --output text)
          echo "AWS caller: ${CALLER_ARN}"
          test "$CALLER_ARN" = "$EXPECTED_ROLE" || {
            echo "Unexpected AWS role. Expected ${EXPECTED_ROLE}"
            exit 1
          }

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Terraform Init
        working-directory: terraform
        run: terraform init -input=false

      - name: Ensure ECR repository exists
        working-directory: terraform
        run: |
          terraform apply -input=false -auto-approve \
            -target=aws_ecr_repository.app \
            -target=aws_ecr_lifecycle_policy.app \
            -target=aws_ecr_repository_policy.lambda_pull \
            -var="aws_region=${AWS_REGION}" \
            -var="project_name=${PROJECT_NAME}" \
            -var="image_tag=${IMAGE_TAG}"

      - name: Login to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set image metadata
        id: meta
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
        run: |
          set -euo pipefail
          test -n "$REGISTRY" || {
            echo "ECR registry output is empty"
            exit 1
          }

          IMAGE_URI="${REGISTRY}/${PROJECT_NAME}"
          IMAGE_REF="${IMAGE_URI}:${IMAGE_TAG}"

          echo "image_uri=${IMAGE_URI}" >> "$GITHUB_OUTPUT"
          echo "image_tag=${IMAGE_TAG}" >> "$GITHUB_OUTPUT"
          echo "image_ref=${IMAGE_REF}" >> "$GITHUB_OUTPUT"

          echo "IMAGE_URI=${IMAGE_URI}" >> "$GITHUB_ENV"
          echo "IMAGE_REF=${IMAGE_REF}" >> "$GITHUB_ENV"

          echo "Using image: ${IMAGE_REF}"

      - name: Validate image metadata
        run: |
          set -euo pipefail
          test -n "${IMAGE_REF:-}" || {
            echo "IMAGE_REF is empty"
            exit 1
          }
          case "$IMAGE_REF" in
            */*:*) ;;
            *)
              echo "Invalid image reference: $IMAGE_REF"
              exit 1
              ;;
          esac

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: |
            ${{ env.IMAGE_REF }}
            ${{ env.IMAGE_URI }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.IMAGE_REF }}
          format: table
          exit-code: "1"
          severity: CRITICAL,HIGH
          ignore-unfixed: true

      - name: Upload Trivy SARIF (informational)
        if: always()
        uses: aquasecurity/trivy-action@v0.36.0
        continue-on-error: true
        with:
          image-ref: ${{ env.IMAGE_REF }}
          format: sarif
          output: trivy-results.sarif
          severity: CRITICAL,HIGH,MEDIUM
          ignore-unfixed: true

      - name: Upload SARIF to GitHub Security
        if: always() && hashFiles('trivy-results.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        continue-on-error: true
        with:
          sarif_file: trivy-results.sarif

      - name: Push image to ECR
        run: |
          docker push "$IMAGE_REF"
          docker push "${IMAGE_URI}:latest"
          echo "Pushed ${IMAGE_REF}"
          echo "Pushed ${IMAGE_URI}:latest"

  deploy:
    name: Deploy Lambda
    needs: build-scan-push
    runs-on: ubuntu-latest
    env:
      IMAGE_TAG: ${{ needs.build-scan-push.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_IAM_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify AWS identity
        run: |
          set -euo pipefail
          EXPECTED_ROLE="arn:aws:iam::124666675812:role/github-actions-bmi-health-check"
          CALLER_ARN=$(aws sts get-caller-identity --query Arn --output text)
          echo "AWS caller: ${CALLER_ARN}"
          test "$CALLER_ARN" = "$EXPECTED_ROLE" || {
            echo "Unexpected AWS role. Expected ${EXPECTED_ROLE}"
            exit 1
          }

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9.8"

      - name: Terraform Init
        working-directory: terraform
        run: terraform init -input=false

      - name: Import existing Lambda invoke permission if needed
        working-directory: terraform
        run: |
          set -euo pipefail
          if terraform state list 2>/dev/null | grep -qx 'aws_lambda_permission.function_url_invoke'; then
            echo "Permission already in Terraform state"
            exit 0
          fi
          if aws lambda get-policy --function-name "${PROJECT_NAME}" 2>/dev/null \
            | grep -q 'AllowPublicInvokeFunction'; then
            terraform import \
              -var="aws_region=${AWS_REGION}" \
              -var="project_name=${PROJECT_NAME}" \
              -var="image_tag=${IMAGE_TAG}" \
              aws_lambda_permission.function_url_invoke \
              "${PROJECT_NAME}/AllowPublicInvokeFunction"
          else
            echo "No existing invoke permission to import"
          fi

      - name: Terraform Apply
        working-directory: terraform
        run: |
          terraform apply -input=false -auto-approve \
            -var="aws_region=${AWS_REGION}" \
            -var="project_name=${PROJECT_NAME}" \
            -var="image_tag=${IMAGE_TAG}"

      - name: Smoke test Function URL
        working-directory: terraform
        run: |
          set -euo pipefail
          URL=$(terraform output -raw function_url)
          URL="${URL%/}"
          echo "Function URL: ${URL}"
          echo "FUNCTION_URL=${URL}" >> "$GITHUB_STEP_SUMMARY"
          curl -fsS "${URL}/health" | tee /tmp/health.json
          curl -fsS "${URL}/bmi?height_cm=175&weight_kg=70" | tee /tmp/bmi.json
          curl -fsS "${URL}/" | tee /tmp/home.html
          python3 - <<'PY'
          import json
          health = json.load(open("/tmp/health.json"))
          bmi = json.load(open("/tmp/bmi.json"))
          home = open("/tmp/home.html").read()
          assert health.get("status") == "ok", health
          assert bmi.get("category") == "normal", bmi
          assert bmi.get("health_advice"), bmi
          assert bmi.get("exercises"), bmi
          assert "BMI Health Check" in home
          print("Smoke tests passed")
          PY
